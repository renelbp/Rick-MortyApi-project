<?xml version="1.0" encoding="utf-8"?>
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/characterListFragmet">

    <fragment
        android:id="@+id/characterListFragmet"
        android:name="com.example.rickandmortyapp.CharacterListFragmet"
        android:label="Characters List"
        tools:layout="@layout/fragment_character_list" >
        <action
            android:id="@+id/action_characterListFragmet_to_characterDetailsFragment"
            app:destination="@id/characterDetailsFragment"

            app:enterAnim="@anim/nav_default_enter_anim"
            app:exitAnim="@anim/nav_default_exit_anim"
            app:popEnterAnim="@anim/nav_default_pop_enter_anim"
            app:popExitAnim="@anim/nav_default_pop_exit_anim" />
    </fragment>
    <fragment
        android:id="@+id/characterDetailsFragment"
        android:name="com.example.rickandmortyapp.CharacterDetailsFragment"
        android:label="Character Details"
        tools:layout="@layout/fragment_character_details" >
        <argument
            android:name="character_selected"
            app:argType="integer"
            android:defaultValue="-1" />
    </fragment>


</navigation>